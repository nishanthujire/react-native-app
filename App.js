import * as React from 'react';
import MainContainer from './navigation/MainContainer';
// import { LogBox } from 'react-native';
// LogBox.ignoreLogs(['Warning: ...']); // Ignore log notification by message    //it will disable all warnings errors show in mobile
// LogBox.ignoreAllLogs();//Ignore all log notifications

function App() {
  return (
    <MainContainer/>
  );
}

export default App;